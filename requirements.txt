# Core ML and NLP libraries
sentence-transformers>=2.3.1   # all-MiniLM-L6-v2 embeddings + optional cross-encoder reranker
faiss-cpu>=1.7.4               # Vector similarity search
transformers>=4.30.2          # HuggingFace transformers (pulled in by sentence-transformers)

# Document processing
pypdf>=3.14.0                 # PDF text extraction (supersedes the deprecated PyPDF2)
nltk>=3.8.1                   # Sentence/word tokenization (also needs punkt_tab on newer versions)

# Web API
flask>=3.0.0                  # Web framework for API
flask-cors>=4.0.0            # CORS support
gunicorn>=21.2.0             # Production WSGI server (dev server is not for production)

# Search and ranking
rank-bm25>=0.2.2             # BM25 keyword scoring

# Utilities
numpy>=1.24.3                # (>=1.26 required for Python 3.12+)
tqdm>=4.65.0                 # Progress bars
requests>=2.31.0             # HTTP requests (used by the download helper)

# Testing
pytest>=7.4.0
